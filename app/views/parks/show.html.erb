<h1><%= @park.name %></h1>
<h2>Admissions : $<%= @park.price %>

<section id="rides-info">
  <% @park.rides.each do |ride| %>
    <p><%= ride.name %>
  <% end %>
  <h3>Average Thrill rating of Rides: <%= @park.ride_average_rating.round(2) %></h3>
</section>